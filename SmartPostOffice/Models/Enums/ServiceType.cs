namespace SmartPostOffice.Models.Enums
{
// An enum is a fixed list of named options.

public enum ServiceType
{
OrdinaryLetter = 1,
OrdinaryParcel = 2,
SpeedPost = 3,
RegisteredMail = 4,
COD = 5 // Cash on Delivery
}
}